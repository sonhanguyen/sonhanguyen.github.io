// import { addParameters } from '@storybook/react'

// addParameters({
//   designToken: {
//     files: preval.require('./scanTokens')
//     // there is a caveat with babel cache, in which the result is not updated if the source
//     // (scanTokens.js) isn't. Workaround:
//     // + rm -rf ./node_modules/.cache/@babel/register
//     // + OR adding random comment in the file
//     // CAN ALSO replace preval with raw-loader:
//     //   github.com/UX-and-I/storybook-design-token
//     //   but we'll lose the ability to customize paths for the tokens
//   }
// })
